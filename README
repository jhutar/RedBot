(*) RedBot pravidla:
	viz RedBot_pravidla.txt

(*) Jak přeložit zdrojový kód serveru:
	make

(*) Typické spuštění serveru:
	./redbot -f default_playfield.txt strategie1/random.sh strategie2/random.sh strategie3/random.sh strategie4/random.sh

(*) Zobrazení historie hry (jak se měnilo území jednotlivých království) - nutné mít nainstalován Gnuplot:
	./historie_zapasu.sh <soubor_s_historii_hry>
